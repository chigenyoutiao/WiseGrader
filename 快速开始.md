# AI作业批改助手 - 快速开始指南

## 📦 项目内容

本项目包含两大核心功能：
1. **AI作业批改系统** - 自动批改学生提交的作业（PDF/DOCX）
2. **学习通自动化** - AI聊天控制、扫码登录、课程抓取

## 🚀 初始化环境

### 1️⃣ 后端环境（Python）

```bash
cd backend
python -m venv .venv
.venv\Scripts\activate          # Windows
pip install -r requirements.txt
```

### 2️⃣ 学习通模块（Python）

```bash
cd xuexitong/cstudy
python -m venv .venv
.venv\Scripts\activate          # Windows
pip install selenium requests   # 核心依赖
```

### 3️⃣ 前端环境（Node.js）

```bash
cd frontend
npm install
```

### 4️⃣ 安装 Redis（必需！）

**Windows 用户**：下载安装 [Memurai](https://www.memurai.com/get-memurai)（Windows 版 Redis）

安装后会自动在后台运行，验证：
```bash
memurai-cli ping
# 应返回: PONG
```

## 🎯 启动服务

**需要 3 个终端窗口同时运行：**

### Terminal 1 - 后端 API
```bash
cd backend
python app.py
```
运行在 `http://localhost:5000`

### Terminal 2 - Celery 任务队列
```bash
cd backend
celery -A celery_worker worker --loglevel=info --pool=solo
```
> ⚠️ Windows 必须使用 `--pool=solo` 参数

### Terminal 3 - 前端开发服务器
```bash
cd frontend
npm run dev
```
运行在 `http://localhost:5173`

## 🧪 测试流程

### 测试 AI 作业批改
1. 访问 `http://localhost:5173`
2. 登录账号：`teacher` / 密码：`admin123`
3. 上传嵌套 ZIP 文件（外层包含多个学生的 ZIP）
4. 设置批改标准
5. 配置 AI 模型和 API Key
6. 点击"开始批改"
7. 等待进度更新
8. 导出 Excel 报告

### 测试学习通功能
1. 登录后进入 AI Agent 页面
2. 使用聊天界面发送指令（如"扫码登录"）
3. AI 会自动控制浏览器操作
4. 查看执行面板的实时进度

## 📁 目录说明

```
Dev4-Package/
├── frontend/              # React 前端（AI 聊天、作业管理界面）
│   ├── src/
│   │   ├── pages/AgentPage.tsx      # AI 聊天页面
│   │   └── components/agent/        # 聊天组件
│   └── package.json
├── backend/               # Flask 后端（API + Celery 任务）
│   ├── app.py            # 主 API
│   ├── celery_worker.py  # 异步任务
│   ├── utils/            # 工具模块（AI批改、ZIP解析等）
│   └── requirements.txt
├── xuexitong/            # 学习通自动化模块
│   └── cstudy/
│       ├── (已拆分)      # Selenium 控制逻辑已迁移到 backend/xxt/*.py
│       └── chromedriver.exe  # Chrome 驱动（19MB）
├── START_ALL_SERVICES.md  # 详细启动指南
└── CLAUDE.md              # 完整项目文档

```

## ⚠️ 常见问题

### Q: 进度条显示 0/0
**A**: Redis 或 Celery Worker 没有运行，请确保两者都已启动

### Q: Celery 启动失败
**A**: Windows 用户必须使用 `--pool=solo` 参数

### Q: chromedriver 报错
**A**: 确保你的 Chrome 浏览器版本与 chromedriver.exe 兼容

### Q: 前端无法连接后端
**A**: 检查后端是否在 5000 端口运行，Celery Worker 是否启动

## 📚 更多文档

- **完整架构说明**：见 `CLAUDE.md`
- **服务启动指南**：见 `START_ALL_SERVICES.md`
- **后端 API 文档**：见 `backend/README.md`

## 🔧 环境变量配置（可选）

可以创建 `.env` 文件配置：
```bash
# 学习通下载目录
XXT_DOWNLOAD_DIR=../xuexitong/cstudy/downloads

# AI API Keys（测试时设置）
AI_GRADER_API_KEY=your-api-key-here

# 代理设置
AI_GRADER_USE_SYSTEM_PROXY=false
```

## 🎉 快速验证

1. 确保 4 个服务都在运行（Redis + Backend + Celery + Frontend）
2. 访问 `http://localhost:5173` 能看到登录页面
3. 后端 API 响应：`curl http://localhost:5000`
4. Redis 连接正常：`memurai-cli ping`

祝测试顺利！如有问题请查看详细文档。
