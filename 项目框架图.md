# Dev4-Package 项目框架图

## 项目概述
**WiseGrader** - 一个智能批改作业的AI系统

这是一个前后端分离的智能作业批改平台，支持从学习通自动下载作业、AI智能批改、格式检查、成绩导出等功能。采用智能代理对话界面，提供自然语言交互体验。

---

## 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户界面层 (Frontend)                      │
│  React + TypeScript + Vite + TailwindCSS + Zustand              │
└────────────────────────────┬────────────────────────────────────┘
                             │ HTTP/REST API
                             │ (Axios)
┌────────────────────────────▼────────────────────────────────────┐
│                      API服务层 (Backend)                          │
│  Flask + Flask-CORS + Python                                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  API路由 (app.py)                                         │  │
│  │  - /api/login          - 用户登录                          │  │
│  │  - /api/upload/jobs    - 上传作业ZIP                      │  │
│  │  - /api/jobs/start     - 启动批改任务                      │  │
│  │  - /api/jobs/status    - 查询任务状态                      │  │
│  │  - /api/jobs/export    - 导出Excel成绩单                  │  │
│  │  - /api/xxt/*          - 学习通集成接口                    │  │
│  │  - /api/agent          - 智能代理对话接口                  │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
┌───────▼────────┐  ┌────────▼────────┐  ┌───────▼────────┐
│  任务队列      │  │   数据存储      │  │  文件处理      │
│  Celery        │  │   Redis/内存    │  │  工具模块      │
│  Worker        │  │   Job Store     │  │   Utils        │
└───────┬────────┘  └─────────────────┘  └───────┬────────┘
        │                                         │
        │                                         │
┌───────▼─────────────────────────────────────────▼────────┐
│              核心业务逻辑层                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  AI批改引擎  │  │  格式检查器  │  │  文件解析器  │  │
│  │ ai_grader.py │  │ doc_parser.py│  │ file_reader  │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└──────────────────────────────────────────────────────────┘
                             │
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                   外部服务集成层                                │
│  ┌──────────────────┐  ┌──────────────────┐                   │
│  │  学习通自动化     │  │  AI模型API       │                   │
│  │  Selenium脚本    │  │  (Kimi/Gemini等) │                   │
│  │  xuexitong/      │  │                  │                   │
│  └──────────────────┘  └──────────────────┘                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 目录结构

```
Dev4-Package/
├── frontend/                    # 前端应用
│   ├── src/
│   │   ├── api.ts              # API客户端封装
│   │   ├── store.ts            # Zustand状态管理
│   │   ├── App.tsx             # 主应用组件
│   │   ├── pages/              # 页面组件
│   │   │   ├── AgentPage.tsx  # 智能代理对话页面（主界面）
│   │   │   ├── LoginPage.tsx   # 登录页
│   │   │   └── RegisterPage.tsx # 注册页
│   │   ├── components/        # UI组件
│   │   │   ├── Sidebar.tsx     # 侧边栏（历史记录）
│   │   │   ├── ConfigSidebar.tsx # 配置侧边栏
│   │   │   ├── LoginModal.tsx  # 登录弹窗
│   │   │   ├── AnimatedLogo.tsx # 动画Logo
│   │   │   ├── MouseFollower.tsx # 鼠标跟随效果
│   │   │   └── agent/          # 代理相关组件
│   │   │       ├── ChatMessage.tsx # 聊天消息组件
│   │   │       ├── ProgressPanel.tsx # 进度面板
│   │   │       ├── GradingResults.tsx # 批改结果
│   │   │       ├── WorkflowStep.tsx # 工作流步骤
│   │   │       └── ExecutionPanel.tsx # 执行面板
│   ├── package.json
│   └── vite.config.ts
│
├── backend/                     # 后端服务
│   ├── app.py                  # Flask主应用
│   ├── celery_worker.py         # Celery异步任务
│   ├── config.py               # 配置文件
│   ├── utils/                  # 工具模块
│   │   ├── ai_grader.py        # AI批改引擎
│   │   ├── doc_parser.py       # 文档格式解析
│   │   ├── file_reader.py      # 文件读取
│   │   ├── zip_handler.py      # ZIP处理
│   │   ├── name_parser.py      # 姓名解析
│   │   └── job_store.py        # 任务存储(Redis/内存)
│   ├── uploads/                # 上传文件目录
│   ├── temp/                   # 临时文件目录
│   └── requirements.txt
│
└── xuexitong/                   # 学习通自动化
    └── cstudy/
        ├── (已拆分)             # Selenium自动化逻辑迁移至 backend/xxt/*.py
        ├── downloads/           # 下载目录
        └── chrome_profile/     # Chrome配置
```

---

## 核心模块详解

### 1. 前端架构 (Frontend)

#### 技术栈
- **框架**: React 19.1.1
- **语言**: TypeScript
- **构建工具**: Vite 7.1.7
- **状态管理**: Zustand 5.0.8
- **UI库**: TailwindCSS + Headless UI
- **HTTP客户端**: Axios 1.13.2
- **图表**: Recharts 3.4.1

#### 主要功能模块

```
Frontend
├── 认证模块
│   └── LoginPage.tsx - 用户登录
│
├── 页面路由
│   ├── AgentPage.tsx - 智能代理对话界面（主界面）
│   ├── LoginPage.tsx - 登录页面
│   └── RegisterPage.tsx - 注册页面
│
├── 核心组件
│   ├── Sidebar.tsx - 侧边栏（历史记录管理）
│   ├── ConfigSidebar.tsx - 配置侧边栏（AI模型、API Key、评分标准）
│   ├── LoginModal.tsx - 登录弹窗
│   ├── AnimatedLogo.tsx - 动画Logo
│   └── agent/ - 智能代理组件
│       ├── ChatMessage.tsx - 聊天消息（支持文件显示）
│       ├── ProgressPanel.tsx - 批改进度面板
│       ├── GradingResults.tsx - 批改结果展示
│       ├── WorkflowStep.tsx - 工作流步骤卡片
│       └── ExecutionPanel.tsx - 执行状态面板
│
└── 状态管理 (store.ts - Zustand)
    ├── 认证状态 (token, isLoggedIn, loginError)
    ├── 任务状态 (jobId, jobStatus, jobResults, jobProgress)
    ├── UI状态 (currentPage, isLoading, isDarkMode, language)
    ├── 代理状态 (messages, agentContext, isAgentLoading, executionSteps)
    ├── 配置状态 (selectedModel, apiKey, criteria, feedbackStyle, gradingTargets)
    ├── 历史记录 (history, activeHistoryId, uploadedJobId)
    └── 学习通状态 (xxtState, xxtPollingTimer)
```

#### API调用流程

```
用户操作
  ↓
Zustand Store Action
  ↓
api.ts (Axios封装)
  ↓
HTTP Request (带Bearer Token)
  ↓
Flask Backend API
  ↓
Response处理
  ↓
更新Store状态
  ↓
UI自动更新
```

---

### 2. 后端架构 (Backend)

#### 技术栈
- **框架**: Flask 3.0.0
- **异步任务**: Celery 5.3.4
- **消息队列**: Redis 5.0.1
- **文件处理**: 
  - pdfplumber (PDF)
  - python-docx (Word)
  - mammoth (DOCX转HTML)

#### API端点分类

```
API Routes (app.py)
│
├── 认证相关
│   └── POST /api/login - 用户登录
│
├── 文件上传
│   ├── POST /api/upload/jobs - 上传作业ZIP（支持在聊天中上传）
│   └── POST /api/upload/criteria-file - 上传评分标准文件
│
├── 任务管理
│   ├── POST /api/jobs/start - 启动批改任务
│   ├── GET  /api/jobs/status/<job_id> - 查询任务状态（支持轮询）
│   └── GET  /api/jobs/export/<job_id> - 导出Excel成绩单
│
├── 评分标准
│   └── POST /api/criteria/extract - AI提炼评分要点
│
├── 学习通集成
│   ├── POST /api/xxt/start - 启动学习通自动化脚本
│   └── GET  /api/xxt/state - 查询脚本执行状态（支持轮询）
│
└── 智能代理
    └── POST /api/agent - 智能对话接口（支持文件上传、上下文管理）
```

#### 核心工具模块

```
backend/utils/
│
├── ai_grader.py          # AI批改核心引擎
│   ├── get_ai_grading()  # 调用AI模型进行批改
│   └── 支持多种模型: Kimi, Gemini, DeepSeek等
│
├── doc_parser.py         # 文档格式检查
│   ├── evaluate_docx_formatting()  # DOCX格式评估
│   └── 检查项: 标题、段落、字体、图表等
│
├── file_reader.py        # 文件内容读取
│   ├── read_file_content()  # 统一文件读取接口
│   └── 支持: PDF, DOCX, DOC, TXT
│
├── zip_handler.py        # ZIP文件处理
│   ├── process_double_layer_zip()  # 双层ZIP解压
│   └── 提取学生作业文件
│
├── name_parser.py        # 学生姓名解析
│   └── 从文件名提取学号和姓名
│
└── job_store.py          # 任务存储
    ├── get_job()         # 获取任务
    ├── save_job()        # 保存任务
    └── 支持Redis和内存两种存储方式
```

---

### 3. 异步任务处理 (Celery)

#### 任务流程

```
用户提交批改任务
  ↓
Flask API接收请求
  ↓
检查Celery Worker可用性
  ↓
┌─────────────────┬──────────────────┐
│  Worker可用      │  Worker不可用     │
│  (异步模式)      │  (同步Fallback)   │
└────────┬────────┴────────┬───────────┘
         │                 │
         │                 │
   提交到Celery队列   后台线程执行
         │                 │
         │                 │
    ┌────▼─────────────────▼────┐
    │  grade_job() 任务执行        │
    │  ┌──────────────────────┐  │
    │  │ 1. 读取学生作业文件   │  │
    │  │ 2. 格式检查(DOCX)     │  │
    │  │ 3. AI批改(重试机制)   │  │
    │  │ 4. 计算最终分数       │  │
    │  │ 5. 保存结果到Redis    │  │
    │  │ 6. 更新任务进度       │  │
    │  └──────────────────────┘  │
    └─────────────────────────────┘
         │
         │
    前端轮询获取状态
```

#### Celery配置
- **Broker**: Redis (localhost:6379)
- **Backend**: Redis (localhost:6379)
- **Worker Pool**: solo (Windows兼容)
- **任务超时**: 10分钟
- **重试机制**: AI调用失败自动重试(最多2-3次)

---

### 4. 学习通自动化集成

#### 自动化流程

```
用户触发学习通连接
  ↓
Flask启动Selenium脚本
  ↓
┌─────────────────────────────────────┐
│  Selenium自动化脚本 (main.py)        │
│  ┌───────────────────────────────┐ │
│  │ 1. 打开Chrome浏览器            │ │
│  │ 2. 访问学习通登录页             │ │
│  │ 3. 显示二维码等待扫码           │ │
│  │ 4. 登录成功后获取课程列表       │ │
│  │ 5. 选择指定课程                │ │
│  │ 6. 获取考试/作业列表           │ │
│  │ 7. 选择指定考试                │ │
│  │ 8. 批量下载学生作业ZIP         │ │
│  │ 9. 解压并上传到后端            │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
         │
         │ 状态更新
         │
   写入状态文件 (runner_state.json)
         │
         │
   前端轮询获取状态
```

#### 状态管理
- **状态文件**: `xuexitong/cstudy/runner_state.json`
- **状态阶段**:
  - `queued` - 排队中
  - `login` - 登录中
  - `courses_loaded` - 课程列表已加载
  - `exams_loaded` - 考试列表已加载
  - `downloading` - 下载中
  - `download_complete` - 下载完成
  - `completed` - 全部完成
  - `error` - 错误

---

### 5. AI批改引擎

#### 批改流程

```
学生作业文件
  ↓
文件内容读取 (file_reader.py)
  ↓
┌─────────────────┬──────────────────┐
│ DOCX文件        │ PDF/TXT文件      │
│ 格式检查        │ 跳过格式检查      │
│ (doc_parser.py) │                  │
└────────┬────────┴────────┬─────────┘
         │                 │
         │                 │
    ┌────▼─────────────────▼────┐
    │  AI批改调用 (ai_grader.py)  │
    │  ┌──────────────────────┐  │
    │  │ 1. 构建Prompt         │  │
    │  │ 2. 调用AI API        │  │
    │  │ 3. 解析返回结果       │  │
    │  │ 4. 提取分数和评语     │  │
    │  │ 5. 错误重试机制       │  │
    │  └──────────────────────┘  │
    └─────────────────────────────┘
         │
         │
    ┌────▼────────────────────────┐
    │ 分数计算                      │
    │ 最终分 = 内容分×0.8 + 格式分×0.2 │
    └──────────────────────────────┘
         │
         │
    保存结果到Redis
```

#### 支持的AI模型
- **Kimi** (Moonshot AI)
- **Gemini** (Google)
- **DeepSeek**
- **其他兼容OpenAI API的模型**

#### 评分维度
1. **内容分** (80%权重)
   - 内容完整性
   - 准确性
   - 创新性
   - 逻辑性

2. **格式分** (20%权重)
   - 标题结构
   - 段落缩进
   - 行间距
   - 字体大小
   - 图表规范
   - 页眉页脚

---

### 6. 数据存储

#### 存储架构

```
任务数据存储
│
├── Redis存储 (优先)
│   ├── Key格式: "job:{job_id}"
│   ├── 数据结构: JSON
│   └── 包含: 任务状态、进度、学生列表、批改结果
│
└── 内存存储 (Fallback)
    ├── 当Redis不可用时使用
    └── 存储在Flask应用内存中
```

#### 任务数据结构

```json
{
  "job_id": "job_12345678",
  "status": "completed",
  "created_at": "2024-01-01T00:00:00",
  "students": [
    {
      "student_id": "2021001",
      "student_name": "张三",
      "file_path": "/path/to/file.docx",
      "file_type": "docx"
    }
  ],
  "progress": {
    "completed": 30,
    "total": 30
  },
  "results": [
    {
      "studentId": "2021001",
      "studentName": "张三",
      "score": 85.5,
      "contentScore": 82,
      "formattingScore": 95,
      "feedback": "作业完成良好...",
      "status": "completed"
    }
  ]
}
```

---

## 数据流图

### 完整批改流程

```
1. 用户上传作业ZIP
   │
   ├─→ Flask接收文件
   ├─→ 解压双层ZIP (zip_handler.py)
   ├─→ 提取学生信息 (name_parser.py)
   └─→ 创建任务并保存 (job_store.py)

2. 用户配置批改参数
   │
   ├─→ 选择AI模型
   ├─→ 输入API Key
   ├─→ 上传/输入评分标准
   └─→ 设置反馈风格

3. 启动批改任务
   │
   ├─→ Flask API接收请求
   ├─→ 检查Celery Worker
   ├─→ 提交任务到队列
   └─→ 返回任务ID

4. Celery Worker处理
   │
   ├─→ 遍历每个学生作业
   │   ├─→ 读取文件内容
   │   ├─→ DOCX格式检查
   │   ├─→ 调用AI批改 (重试机制)
   │   ├─→ 计算最终分数
   │   └─→ 保存结果到Redis
   │
   └─→ 更新任务状态

5. 前端轮询状态
   │
   ├─→ 每2秒查询任务状态
   ├─→ 更新UI显示进度
   └─→ 批改完成后显示结果

6. 导出成绩单
   │
   ├─→ 前端请求导出
   ├─→ 后端读取结果数据
   ├─→ 生成Excel文件 (pandas)
   └─→ 返回文件下载
```

---

## 技术特点

### 1. 前后端分离
- RESTful API设计
- JWT Token认证
- CORS跨域支持

### 2. 异步处理
- Celery任务队列
- Redis消息中间件
- 支持大规模并发批改

### 3. 容错机制
- AI调用失败自动重试
- Redis不可用时内存Fallback
- Celery不可用时同步Fallback

### 4. 智能代理对话
- 自然语言对话界面（类似ChatGPT）
- 支持文件上传（最多2个，显示在聊天框内）
- 对话历史记录管理
- 学习通自动化集成
- 一键式批改流程
- 多语言支持（中文/英文）

### 5. 格式检查
- DOCX文档格式自动评估
- 多维度格式评分
- 格式问题可视化展示

---

## 部署架构

```
┌─────────────────────────────────────────┐
│          前端服务 (Vite Dev Server)      │
│          http://localhost:5173          │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│         后端服务 (Flask)                │
│          http://localhost:5000          │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│         Celery Worker                   │
│         celery -A celery_worker worker  │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│         Redis (Memurai)                 │
│         localhost:6379                  │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│         Chrome + Selenium               │
│         (学习通自动化)                   │
└─────────────────────────────────────────┘
```

---

## 关键配置文件

### 后端配置 (config.py)
- 上传目录: `backend/uploads/`
- 临时目录: `backend/temp/`
- 管理员账号: `teacher/admin123`
- 文件大小限制: 100MB
- 格式评分权重: 内容80% + 格式20%

### 前端配置 (vite.config.ts)
- 开发服务器: `http://127.0.0.1:5173`
- API代理: `http://127.0.0.1:5000`
- 构建输出: `frontend/dist/`

---

## 核心功能特性

### 智能代理对话界面
- **自然语言交互**: 用户通过对话方式操作，无需复杂配置
- **文件上传**: 支持在聊天中上传文件（ZIP作业包、评分标准等），最多2个文件
- **历史记录**: 自动保存对话历史，支持查看、删除、切换对话
- **工作流可视化**: 实时显示批改进度和执行步骤
- **多语言支持**: 中英文界面切换

### 文件处理
- **双层ZIP解压**: 自动处理学习通下载的嵌套ZIP结构
- **多格式支持**: PDF、DOCX、DOC、TXT等格式
- **格式检查**: DOCX文档自动格式评估（标题、段落、字体等）
- **文件预览**: 上传后立即在聊天框内显示文件信息

### 批改功能
- **多模型支持**: DeepSeek、Kimi、GPT-4o、Gemini等
- **智能评分**: 内容分（80%）+ 格式分（20%）
- **批量处理**: 异步任务队列，支持大规模批改
- **结果导出**: Excel格式成绩单导出

## 总结

这是一个功能完整的智能作业批改系统，具有以下优势：

1. **自动化程度高**: 支持从学习通自动下载作业
2. **批改效率高**: 异步任务处理，支持大规模批改
3. **用户体验好**: 智能代理对话界面，操作简单直观
4. **功能完善**: 格式检查、成绩统计、Excel导出、历史记录
5. **扩展性强**: 支持多种AI模型，易于集成新功能

---

*最后更新: 2025年1月*
